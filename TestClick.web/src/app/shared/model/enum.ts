export enum responseStatuEnum {
    success = 'Success',
    failure = 'Failure',
    notFound = 'NotFound',
    validationError = 'ValidationError',
    unauthorized = 'Unauthorized',
    forbidden = 'Forbidden',
    serverError = 'ServerError'
}

export enum screenTypeEnum {
    Analog = 1,
    Digital = 2
}

export enum screenPlacementTypeEnum {
    Indoor = 1,
    Outdoor = 2
}

export enum screenStatusEnum {
    Active = 1,
    Inactive = 2
}

